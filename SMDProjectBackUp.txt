//SMD Project Fall 2017
//Authors: Alec Goodner, Thatcher Rickertsen
//COMP 3700

/**
	Might be useful: https://www.tutorialspoint.com/javaexamples/java_simple_gui.htm (GUI Stuff)
    Also may be useful: https://www.tutorialspoint.com/javaexamples/java_jdbc.htm (JDBC Stuff)
*/
//GUI Stuff
import java.awt.Font;
import java.awt.Graphics;
import java.awt.Graphics2D;
import java.awt.RenderingHints;
import javax.swing.JFrame;
import javax.swing.JPanel;
//SQL Stuff
import java.sql.*;

/**
	Connects mySQL Database to Jave project... Or somthing...
*/
class MysqlCon {
  public static void main(String args[]) {
    try {
    	Class.forName("com.mysql.jdbc.Driver");
      Connection con=DriverManager.getConnection(
      	"jdbc:mysql://localhost:3306/example","root","root");
      	//example database name, root is username & password
      Statement stmt = con.createStatement();
      ResultSet rs = stmt.executeQuery("SELECT * FROM Product");
      while(rs.next())
        System.out.println(rs.getInt(1)+" "+rs.getString(2)+" "+rs.getDouble(3));
      con.close();
    }
    catch(Exception e) {
    	System.out.println(e);
    }
  }
}

private class System {
  public void login() {
  }
}

public class Customer {
	private int[] cardInformation = new int[3];
  	private Product[] checkoutList;
}

public class Product 
  
/**
	Draws text using GUI, should result in "Text" being drawn into a frame
*/
public class Main extends JPanel {
	public void paint(Graphics g) {
    	Graphics2D g2 = (Graphics2D)g;
        g2.setRenderingHint(RenderingHints.KEY_ANTIALIASING, RenderingHints.VALUE_ANTIALIAS_ON);
      	
      	//Font settings
		Font font = new Font("Serif", Font.PLAIN, 96);
      	g2.setFont(font);
		//Text String
      	g2.drawString("Text", 40, 120);
	}
  	public static void main(String[] args) {
		JFrame f = new JFrame();
      	f.getContentPane().add(new Main());
      	f.setSize(300, 200);
      	f.setVisible(true);
   }
}
